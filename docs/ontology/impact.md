# Impact

`impact_of_type` walks reverse indexes: feature types, Specialize descendants, importing ontology packages.

Example: changing `Mass` reaches every property typed `Mass` and packages that depend on `@os20/physics`.

SQL indexes would only accelerate these maps. Decisions stay on IDs.
